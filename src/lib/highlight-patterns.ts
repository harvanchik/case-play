export const HIGHLIGHT_PATTERNS = {
	accept: /\b(?:accept|accepts|accepted)\b/gi,
	decline: /\b(?:decline|declines|declined)\b/gi,
	offset: /\b(?:offset|offsets|offsetted)\b/gi,
	open: /\b(?:open|opens|opened)\b/gi,
	closed: /\b(?:close|closes|closed)\b/gi
} as const;
